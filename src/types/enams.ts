// TODO: Всё что ниже примеры

export enum Unit {
  characters = 'characters',
  planets = 'planets'
}

export enum ModalKey {
  Confirmation,
  Planet
}

export enum Keyboard {
  Enter = 'Enter',
  Space = 'Space',
  ArrowUp = 'ArrowUp',
  ArrowDown = 'ArrowDown',
  Escape = 'Escape'
}

export enum LocalStorageKey {
  favoriteCards = '_favoriteCards'
}
